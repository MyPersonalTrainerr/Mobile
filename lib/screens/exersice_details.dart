import 'package:flutter/material.dart';

class ExersiceDetailsScreen extends StatelessWidget {
  const ExersiceDetailsScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}